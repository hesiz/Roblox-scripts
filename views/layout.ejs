<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= typeof title !== 'undefined' ? title + ' | ' : '' %>Roblox Scripts</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <header class="site-header">
      <div class="container header-inner">
        <a href="/" class="logo">Roblox Scripts</a>
        <nav class="nav">
          <a href="/">Inicio</a>
          <a href="/admin">Admin</a>
        </nav>
      </div>
    </header>
    <main class="container main">
      <aside class="sidebar">
        <h3>Categorías</h3>
        <ul>
          <% categories && categories.forEach(c => { %>
            <li><a href="/categoria/<%= c.slug %>"><%= c.name %></a></li>
          <% }) %>
        </ul>
      </aside>
      <section class="content">
        <%- body %>
      </section>
    </main>
    <footer class="site-footer">
      <div class="container">
        © <%= new Date().getFullYear() %> Roblox Scripts. Todos los derechos reservados.
      </div>
    </footer>
  </body>
  
</html>
